{
  "name": "@creatorcanon/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared build + lint configs (tsconfig presets, eslint presets).",
  "exports": {
    "./tsconfig/base.json": "./tsconfig/base.json",
    "./tsconfig/node.json": "./tsconfig/node.json",
    "./tsconfig/nextjs.json": "./tsconfig/nextjs.json",
    "./tsconfig/react-lib.json": "./tsconfig/react-lib.json",
    "./eslint/base.js": "./eslint/base.js",
    "./eslint/next.js": "./eslint/next.js"
  },
  "files": ["tsconfig", "eslint"],
  "scripts": {
    "lint": "echo 'config: no lint'",
    "typecheck": "echo 'config: no typecheck'",
    "test": "echo 'config: no tests'",
    "build": "echo 'config: no build'",
    "clean": "echo 'config: nothing to clean'"
  }
}
