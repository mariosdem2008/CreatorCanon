{
  "name": "@creatorcanon/cost-ledger",
  "version": "0.0.0",
  "private": true,
  "description": "Per-stage cost accounting across OpenAI + Gemini + Stripe + Cloudflare.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import \"../../node_modules/.pnpm/tsx@4.19.1/node_modules/tsx/dist/loader.mjs\" --test src/*.test.ts",
    "build": "echo 'cost-ledger: source-only, no build'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@creatorcanon/core": "workspace:*",
    "@creatorcanon/db": "workspace:*"
  },
  "devDependencies": {
    "@creatorcanon/config": "workspace:*",
    "typescript": "5.6.2"
  }
}
