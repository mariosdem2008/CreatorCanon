{
  "name": "@atlas/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared domain types, zod schemas, result/error types, invariants.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./errors": "./src/errors.ts",
    "./env": "./src/env.ts",
    "./pipeline-stages": "./src/pipeline-stages.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'core: no tests yet'",
    "build": "echo 'core: source-only, no build'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@atlas/config": "workspace:*",
    "typescript": "5.6.2"
  }
}
