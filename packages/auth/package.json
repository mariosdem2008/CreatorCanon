{
  "name": "@atlas/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Auth.js v5 configuration, Google OAuth provider, session helpers.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'auth: no tests yet'",
    "build": "echo 'auth: source-only, no build'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@atlas/core": "workspace:*",
    "@atlas/db": "workspace:*"
  },
  "devDependencies": {
    "@atlas/config": "workspace:*",
    "typescript": "5.6.2"
  }
}
