/**
 * Worksheet Forge — Task A.3 placeholder.
 */

import type { CodexClient, ComposeInput, WorksheetComponent } from '../types';

export async function composeWorksheets(
  _input: ComposeInput,
  _opts: { codex: CodexClient },
): Promise<WorksheetComponent[]> {
  throw new Error('composeWorksheets: not yet implemented (Task A.3)');
}
