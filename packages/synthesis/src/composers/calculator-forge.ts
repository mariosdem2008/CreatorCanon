/**
 * Calculator Forge — Task A.4 placeholder.
 */

import type { CalculatorComponent, CodexClient, ComposeInput } from '../types';

export async function composeCalculators(
  _input: ComposeInput,
  _opts: { codex: CodexClient },
): Promise<CalculatorComponent[]> {
  throw new Error('composeCalculators: not yet implemented (Task A.4)');
}
