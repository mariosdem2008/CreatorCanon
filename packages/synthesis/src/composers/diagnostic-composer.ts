/**
 * Diagnostic composer — Task A.5 placeholder.
 */

import type { CodexClient, ComposeInput, DiagnosticComponent } from '../types';

export async function composeDiagnostic(
  _input: ComposeInput,
  _opts: { codex: CodexClient },
): Promise<DiagnosticComponent> {
  throw new Error('composeDiagnostic: not yet implemented (Task A.5)');
}
