# Dependencies
node_modules/
.pnpm-store/

# Build output
.next/
dist/
build/
# `out/` here is intentional — it covers Next.js static export. The Drizzle
# migration directory `packages/db/drizzle/out/` is unignored below.
out/
*.tsbuildinfo

# Drizzle migration output is checked in (overrides the broader `out/` rule above)
!packages/db/drizzle/out/

# Turbo
.turbo/

# Environment
.env
.env.local
.env.*.local
.env.development.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Testing
coverage/
.nyc_output/

# IDE
.vscode/*
!.vscode/settings.example.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# OS
.DS_Store

# Sentry
.sentryclirc

# Trigger.dev
.trigger/

# Playwright
playwright-report/
test-results/
blob-report/
playwright/.cache/

# Local state
.vercel
.local/
*.local

# Claude Code + Superpowers session state — ignore everything EXCEPT skills.
# The skills under .claude/skills/pipeline/ are the editorial source of truth
# and must be versioned. Git's pattern semantics require unignoring the parent
# directory (`.claude/*`) before child re-include rules can take effect.
.claude/*
!.claude/skills/
!.claude/skills/**
.superpowers/
.worktrees/
