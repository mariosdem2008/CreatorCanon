# ---------------------------------------------------------------------------
# Channel Atlas — environment template
# Copy to `.env` at the repo root. Each app reads from this file via dotenv.
# Full wiring lands in ticket 0.6. Keys listed now so the schema is stable.
# ---------------------------------------------------------------------------

NODE_ENV=development

# --- Public (exposed to the browser) ----------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_HUB_ROOT_DOMAIN=atlas.local
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://eu.posthog.com

# --- Postgres (Neon) --------------------------------------------------------
DATABASE_URL=postgres://postgres:postgres@localhost:5432/atlas
DATABASE_POOL_MAX=10

# --- Redis (Upstash in prod, local docker in dev) ---------------------------
REDIS_URL=redis://localhost:6379

# --- Cloudflare R2 ----------------------------------------------------------
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=atlas-dev
R2_PUBLIC_BASE_URL=https://cdn.atlas.local

# --- Auth.js v5 + Google OAuth ---------------------------------------------
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# --- YouTube OAuth (separate client — asks for YT scopes, not sign-in only)
YOUTUBE_OAUTH_CLIENT_ID=
YOUTUBE_OAUTH_CLIENT_SECRET=

# --- AI providers -----------------------------------------------------------
OPENAI_API_KEY=
GEMINI_API_KEY=

# --- Stripe -----------------------------------------------------------------
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# --- Email (Resend) ---------------------------------------------------------
RESEND_API_KEY=

# --- Observability ----------------------------------------------------------
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=channel-atlas-web

# --- Trigger.dev ------------------------------------------------------------
TRIGGER_SECRET_KEY=
TRIGGER_API_URL=https://api.trigger.dev
